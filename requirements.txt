# vantage is stdlib-only — nothing is required to run the pipeline.
#
# The recon/enum tooling (nmap, gobuster, nikto, searchsploit, ...) are system
# packages installed via setup.sh, not pip dependencies. Run `vantage --check`
# to see what's present.
